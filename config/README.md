# Конфигурация DronT16

Единый формат конфигурационных файлов проекта — TOML.

Каждый параметр должен иметь комментарий на русском языке. Значения по
умолчанию и безопасные ограничения хранятся рядом с параметрами. Секреты,
ключи и персональные данные в конфигурационные файлы проекта не добавляются.

Файл `follow.toml` содержит настройки модуля сопровождения. При добавлении
нового модуля его конфигурация также создаётся в формате TOML с русскими
комментариями.

Секции `[msp]` и `[landing]` в `bridge.toml` предназначены для отдельного
канала чтения датчиков Betaflight и стендового контроллера посадки. В текущем
повторном стендовом тесте MSP включён, а `landing.enabled` оставлен выключенным
до отдельной проверки без винтов; после безопасного испытания посадку можно
включить.

Свежесть ATTITUDE и ALTITUDE проверяется раздельно. Параметр
`stabilization_channel = 0` отключает автоматическое переключение
ANGLE/HORIZON; включать AUX можно только после проверки Betaflight.

При ARM модуль фиксирует высоту `ALTITUDE_ZERO`. Параметры
`level_roll_tolerance_deg`, `level_pitch_tolerance_deg` и `level_hold_s` не
разрешают снижение, пока дрон не выровнен и не удерживает горизонт заданное
время.

Утверждённый MSP-порт Raspberry — `/dev/ttyAMA1` через GPIO0/1, физические
pin 27/28; текущий CRSF остаётся на `/dev/ttyAMA0`, pin 8/10.

Файл `osd.toml` содержит размеры центрального квадрата, перекрестия, рамок,
точки цели и подписей экранного OSD. Для уменьшения квадрата изменяется
параметр `osd.capture_box_size`.

В разделе `[osd.output]` параметр `video_standard` задаёт единый стандарт
аналогового тракта. Для текущего входа EasyCap от Betaflight используется
`NTSC`: Raspberry выбирает J7 строго в режиме `720x480i`. Не менять его на
`PAL`, пока источник Video OUT полётного контроллера остаётся NTSC.
